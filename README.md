Explanition:

The main cpp file is loacted in "src" and is called "main.cpp" -

(Explanition about the client will be in the client README)

This file includes of a server program (built in a low end coding language C++ ) that can be & was uploaded into a micro controller
such as an "ESP32", which is able to comunicate along with a computer using wifi.

this side of the program recives deta from the client side and turns that deta to real world inputs
like changing the angle of the servo
and changing the (simulated) throttle to make the car move (in simulation)

===============================================================================================================================
The purpose of this project is to demonstrate successful communication between a server and an infinte amount of clients.
These are built in two different coding languages and run on two completely different types of computers,
communicating over wifi to apply results as real-world data and movement.
